import React from 'react';
import './styles.scss';

const Header = (props) => {
    return (
        <header data-test="headerComponent">
            <h4>Rahul</h4>
        </header>
    )
};

export default Header;